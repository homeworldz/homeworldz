[server]
address = 127.0.0.1:8002
public_url = https://grid.homeworldz.com

[grid]
name = Homeworldz
# Ordered new-arrival list (docs/CLIENT2.md): where a user with no stored
# location lands, first entry preferred. Region/x/y/z, comma-separated.
# welcome_locations = Welcome/127/127/23,Welcome/129/129/23
# The once-per-login grid greeting ({grid}/{user} placeholders; empty disables):
# welcome_message = Welcome to {grid}, {user}!

[client]
url = http://127.0.0.1:8002

[auth]
# Replace this with a long random value shared only with this grid's regions.
service_token = change-me

# Browser-facing website API (homeworldz-api), reverse-proxied by Caddy as
# api.homeworldz.com -> 127.0.0.1:8003 (see deploy/linux/Caddyfile.api).
[website]
address = 127.0.0.1:8003
# Public base URL of this API; the probe derives the grid channel's wss://
# URL from it. Leave unset if the deployment has no public name.
public_url = https://api.homeworldz.com
allowed_origins = https://homeworldz.com
# Replace this with a long random value; rotating it invalidates all sessions.
jwt_secret = change-me
token_ttl_seconds = 3600

[mail]
transport = smtp
from = welcome@homeworldz.com
verification_url = https://homeworldz.com/verify
# SMTP relay used when transport = smtp. These values are for Cloudflare Email
# Sending, which requires implicit TLS on port 465 with the literal username
# "api_token" and a Cloudflare API token as the password.
smtp_host = smtp.mx.cloudflare.net
smtp_port = 465
smtp_username = api_token
smtp_password = change-me
smtp_implicit_tls = true

# Asset vault (ADR 0026): durable storage for the bytes behind every
# inventory-referenced asset, so a user's inventory survives the loss of any
# region. Use an absolute path on a service host, on storage that is backed up.
[vault]
path = /var/lib/homeworldz/vault
